const createCards = (content = {}) => {

    //Reviso que el objeto no este vacio. 
    if (Object.keys(content).length === 0){
        console.error("No puedo renderizar una card vacia sin contenido");
        return;
    }
    
}